apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
    version: blue   # 🔁 Jenkins flips this
  ports:
  - name: http
    port: 5000
    targetPort: 5000

